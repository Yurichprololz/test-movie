import { SetImageAsBgDirective } from './set-image-as-bg.directive';

describe('SetImageAsBgDirective', () => {
  it('should create an instance', () => {
    const directive = new SetImageAsBgDirective();
    expect(directive).toBeTruthy();
  });
});
